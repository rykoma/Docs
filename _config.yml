# Hexo Configuration

# Site
# `title` is the fallback used when a page's language cannot be determined
# (e.g. the root language-selector page). Language-specific titles are
# defined under `titles` and take priority whenever the current page has a
# known language.
title: Developer Messaging 研究所
titles:
  ja: Developer Messaging 研究所
  en: Developer Messaging Lab
rss_descriptions:
  ja: Developer Messaging 研究所の最新記事
  en: The latest posts from Developer Messaging Lab
subtitle: ''
description: ''
keywords:
author: Ryutaro Koma
language: ja
timezone: Asia/Tokyo

# URL
url: https://blog.rykoma.net
root: /
permalink: :slug/
permalink_defaults:
pretty_urls:
  trailing_index: true
  trailing_html: true

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing
new_post_name: :title.md
default_layout: post
titlecase: false
external_link:
  enable: true
  field: site
  exclude: ''
filename_case: 0
render_drafts: false
post_asset_folder: true
relative_link: false
future: true
syntax_highlighter: highlight.js
highlight:
  line_number: true
  auto_detect: false
  tab_replace: ''
  wrap: true
  hljs: false
prismjs:
  preprocess: true
  line_number: true
  tab_replace: ''

# Home page setting
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# Category & Tag
default_category: uncategorized
category_map:
tag_map:

# Metadata elements
meta_generator: true

# Date / Time format
date_format: YYYY-MM-DD
time_format: HH:mm:ss
updated_option: 'mtime'

# Pagination
per_page: 10
pagination_dir: page

# Include / Exclude file(s)
include:
exclude:
ignore:

# Extensions
theme: landscape

# Deployment
deploy:
  type: ''
